import { AlteraTextoDirective } from './altera-texto.directive';

describe('AlteraTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlteraTextoDirective();
    expect(directive).toBeTruthy();
  });
});
